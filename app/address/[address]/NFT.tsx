async function getNFTImage(id: string) {
  return "not supported as of now";
}

async function getSpamClassification(id: string) {
  return "not supported as of now";
}

export default async function NFT({ id }: { id: string }) {
  
  return "not supported as of now";
}
